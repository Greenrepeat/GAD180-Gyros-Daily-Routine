using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveCam : MonoBehaviour
{
    //public GameObject player;
    //public float camSpeed = 5f;

    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (Input.GetKey(KeyCode.W) && transform.position.y < 2)
    //    {
    //        transform.Translate(Vector2.up * Time.deltaTime * camSpeed);
    //    }
    //    else if (Input.GetKey(KeyCode.S) && transform.position.y > -4   )
    //    {
    //        transform.Translate(Vector2.down * Time.deltaTime * camSpeed);
    //    }
    //}
}
